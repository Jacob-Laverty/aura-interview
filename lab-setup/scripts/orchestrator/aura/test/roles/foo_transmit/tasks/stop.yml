# ------------------------------------------------------------------------------
# stop.yml
#
# Stop the foo-transmit service.
# ------------------------------------------------------------------------------
---
- name: Start Service
  ansible.builtin.command:
    cmd: service foo-transmit start

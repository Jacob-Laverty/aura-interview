# ------------------------------------------------------------------------------
# start.yml
#
# Start the foo-transmit service.
# ------------------------------------------------------------------------------
---
# call the task without any variables.
- name: Start Service
  ansible.builtin.service:
    name: foo-transmit
    state: started

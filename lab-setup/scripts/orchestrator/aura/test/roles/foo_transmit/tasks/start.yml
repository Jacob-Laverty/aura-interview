# ------------------------------------------------------------------------------
# start.yml
#
# Start the foo-transmit service.
# ------------------------------------------------------------------------------
---
# call the task without any variables.
- name: Start Service
  ansible.builtin.command:
    cmd: service foo-transmit start
